/* =========================================================
   SISTEMA DE REPORTES - ASUNTOS INTERNOS
   Listado - Puente del módulo Editar
========================================================= */

import {
    inicializarEditarReporte,
} from './editar/editar.js';


document.addEventListener(
    'DOMContentLoaded',
    () => {

        inicializarEditarReporte();

    }
);